
global string _s1_$ | -> Aqui se corrigio un sintactico

float _mi_  є int _dif_, char _otra_ э ¿  |sintactico  э 
	let int _otra_$
	let string _str_="Hola $~&|?$& э  mundo"$
	for  _i_=20 step -2 downto 0 do  | puede dar sintactico
	 ¿ 
		loop
		  	_var_2 = _var_2 - 1$ |semantico
			_var_ = _var_ - 1$ |semantico
		exit when є _var2_>12.2 @ є12> єΣє 34+35 эээ  э $ |semantico NOT
		end loop$

		| De esta parte de aqui falta toda la estructura del DECIDE OF
		¡else ->  ¿  | sintactico
			input є _var2_ э $
			break$
		 ? !

		decide   of
		є _var_==0 э  ->
		 ¿ 
			output є "entra al if" э $
		 ?
		end decide$
		decide   of 
		є true э ->  | A este le faltan parentesis y por eso da error sintactico
		 ¿ 
			output є _var_ э $
		 ? 
		else | ->   La fecla no la usa el else, nada mas pasa al bloque.
		 ¿ 
			decide   of  |semantico _otra_
				є _otra_  == 1э -> ¿ | lo de _otra_ estaba sin parentesis y eso no es valido para nuestro lenguaje
					output є "!|" э $ ? 
				є _otra_  == 2 э -> ¿
					_otra_ = 10$
					break$ ?
				else  ¿ | sintactico: Se quita la flecha y se intercambia posisiones con la condicion anterior.
					_otra_ = -10.5 * -1.9$ ?
			end  decide  $

			decide   of  |semantico _otra2_
				є _otra2_ == 10 э -> ¿ 
					output є "un case" э $ ?
			end  decide  $ 
		 ?
		 end  decide  $
	 ? 
	return 1$ |semantico
  ? 

 boolean _miOtraFun_  є  э   ¿ 
	return -5.6$ |semantico
  ? 

 void principal є э  ¿ | semantico tipo int, sintactico? de aqui se tiene un parametro en el principal y este no acepta parametros.
	_arr_[67+_i_++] = -0.01$  | semantico
	let boolean _b1_$ |semantico
	let string _s1_$ |semantico

	|input є _s1_ э $ |semantico -> El input del profe es diferente al de nosotros, el de nostros es de asignacion y requiere del tipo.
	let int entrada = input є int э$ | Este es el correcto.
	output є _b1_ э $ |semantico
	output є true э $ |semantico
	output є -6.7 э $
	return$  |sintactico
	return 1+_f_ є  э $
 ? 